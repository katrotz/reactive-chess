import styled from 'styled-components/native';

export const CemeteryView = styled.View`
    flexDirection: row;
    height: 30px;
    alignItems: center;
    justifyContent: center;
`;

export const FallbackText = styled.Text`
    color: #cccccc;
    textAlign: center;
`;
